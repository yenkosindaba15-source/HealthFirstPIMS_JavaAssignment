CREATE TABLE suppliers(
    supplier_id INT PRIMARY KEY AUTO_INCREMENT,
    name: VARCHAR(100),
    contact_person: VARCHAR(100),
    phone: VARCHAR(20),
    email: VARCHAR(100),
    address: VARCHAR(100)
);